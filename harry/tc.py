#conversion of one type of data type into another data type is called typecasting
a="1"
b="2"
print(a+b)   #since they both are string we didn't get the result as 3, instead the strings are concatenated

#explicit:done by the developer 
#implicit:done by the python itself